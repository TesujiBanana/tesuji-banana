var React = require('react');
var AuthView = require('../../src/components/auth_view.jsx');

describe('foobar', function() {
  beforeEach(function() {
    this.foo = 'bar';
  });
  
  it('should be oooookay', function() {
    expect(this.foo).to.be('bar');
  });
})
