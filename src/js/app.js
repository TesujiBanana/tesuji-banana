'use strict';

var React = require('react');

var AppView = require('./components/app_view.jsx');

React.render(React.createElement(AppView), document.getElementById('viewport'));

module.exports = {};
